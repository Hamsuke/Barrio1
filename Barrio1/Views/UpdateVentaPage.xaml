<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Barrio1.Views.UpdateVentaPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:model="clr-namespace:Barrio1.Models"
    xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
    xmlns:viewmodel="clr-namespace:Barrio1.ViewModels"
    x:DataType="viewmodel:UpdateVentaViewModel"
    >

    <VerticalStackLayout Margin="20">
        <Grid ColumnDefinitions="200, 115" RowDefinitions="*,*,*,*" RowSpacing="10">

            <Label Text="Saldo pagado" Grid.Column="0" Grid.Row="0"/>
            <Label Text="{Binding Venta.pago}" Grid.Column="1" Grid.Row="0"/>
            <Label Text="Costo de venta" Grid.Column="0" Grid.Row="1"/>
            <Label Text="{Binding Venta.costo}" Grid.Column="1" Grid.Row="1"/>
            <Label Text="Pago" VerticalTextAlignment="Center" Grid.Column="0" Grid.Row="2"/>
            
            <Picker Grid.Column="1" Grid.Row="2" Title="Metodo de pago" SelectedItem="{Binding Venta.metodo}">
                <Picker.Items>
                    <x:String>Tarjeta</x:String>
                    <x:String>Transferencia</x:String>
                    <x:String>Efectivo</x:String>
                </Picker.Items>
            </Picker>
            <Entry Text="{Binding Tmp}" Grid.Row="3"/>
        </Grid>

        <Button
            Command="{Binding UpdateVentaCommand}"
            Text="Actualizar"
            />
    </VerticalStackLayout>
</ContentPage>